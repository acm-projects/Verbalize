"use client";
import { motion } from "framer-motion";

// This is your reusable "useEffect-style" logic
export default function Popup({ children, delay = 0 }: { children: React.ReactNode, delay?: number }) {
  
}